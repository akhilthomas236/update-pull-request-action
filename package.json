{
  "name": "pr-comment-action",
  "version": "1.0.0",
  "description": "GitHub Action to add a comment to a PR",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "jest",
    "lint": "eslint .",
    "prepare": "npm run build"
  },
  "keywords": [
    "github",
    "actions",
    "pull request",
    "comments",
    "automation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "eslint": "^8.39.0",
    "jest": "^29.5.0"
  }
}
